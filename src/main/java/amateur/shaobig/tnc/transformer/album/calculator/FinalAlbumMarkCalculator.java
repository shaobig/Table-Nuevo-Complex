package amateur.shaobig.tnc.transformer.album.calculator;

import java.math.BigDecimal;

public interface FinalAlbumMarkCalculator {

    BigDecimal calculate(BigDecimal averageMark, BigDecimal sum);

}
